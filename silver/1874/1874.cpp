#include <iostream>

using namespace std;

int main() {
  ios_base::sync_with_stdio(false); cin.tie(NULL);

  freopen("example.txt", "r", stdin);

  int N, n;

  cin >> N;

  int T[N], O[N * 2],top = -1;

  for (int i=1; i<=N; i++) {
    cin >> n;

    // TODO: 처리하기
    
  }

  return 0;
}